# FastAPI and web server
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Celery and Redis
celery==5.3.4
redis==5.0.1

# ML and NLP libraries
transformers==4.35.0
# Note: PyTorch CPU-only is installed separately in Dockerfile for optimization
# torch and torchvision are installed via --index-url in Dockerfile
langdetect==1.0.9
numpy<2.0.0  # Pin to NumPy 1.x for compatibility
deep-translator==1.11.4  # Translation layer for Turkish to English

# PII masking
presidio-analyzer==2.2.33
presidio-anonymizer==2.2.33
spacy>=3.4.0,<4.0.0

# Database
sqlalchemy==2.0.23

# Additional dependencies
python-multipart==0.0.6
slowapi==0.1.9  # Rate limiting for FastAPI

# Testing
pytest==7.4.3
httpx==0.25.2  # Required by FastAPI TestClient
locust==2.17.0  # Load testing framework


